name: Build and Deploy Code

on:
  push:
    branches:
      - "master"
    pull_request:
      branches:
        - "test_branch"
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: pulling git repo
        uses: actions/checkout@v2
      - name: say hi to karim
        run: echo "hello karim"